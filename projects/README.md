# Local RPG Maker MV Projects

把你的本地 RPG Maker MV 工程放在这个目录下，例如：

```text
projects/my-game/
```

工程目录需要包含 `data/` 或 `www/data/`。这些工程只用于本机开发、测试和 Agent 任务执行，默认不会进入 Git，也不会随 RPG-Agent-MV 发布。

不要把正式游戏工程、导出包、存档或运行时文件提交到产品仓。
