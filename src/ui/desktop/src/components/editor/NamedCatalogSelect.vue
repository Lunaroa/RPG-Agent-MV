<template><label>{{ label }}<select :value="value" @change="$emit('change',Number(($event.target as HTMLSelectElement).value))"><option v-for="entry in options" :key="entry.id" :value="entry.id">{{ entry.id.toString().padStart(4,'0') }} {{ entry.name }}</option></select></label></template><script setup lang="ts">import type{NamedCatalogEntry}from'../../api/client';defineProps<{label:string;value:number;options:NamedCatalogEntry[]}>();defineEmits<{change:[value:number]}>();</script>
